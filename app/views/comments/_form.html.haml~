= form_for([@blog_entry, @blog_entry.comments.build]) do |f|
  .field
    = f.label :name
    %br/
    = f.text_field :name
  .field
    = f.label :body
    %br/
    = f.text_area :body
  .actions
    = f.submit
